#include "precompile_header.h"

#include "CompoundRecord.h"

/*CompoundRecord::CompoundRecord(const std::string stringCompoundRecord)
{
   std::vector <std::string> partsOfStringCompoundRecord;
   boost::split(partsOfStringCompoundRecord, stringCompoundRecord, boost::is_any_of(" "));

   _compoundId = partsOfStringCompoundRecord[0];
   for (auto feature = partsOfStringCompoundRecord.cbegin(); feature != partsOfStringCompoundRecord.cend(); ++feature)
   {
      if (feature != partsOfStringCompoundRecord.cbegin()
          && feature != (--partsOfStringCompoundRecord.cend()))
      {
         boost::lexical_cast<std::string>(feature
      }
   }
   //todo: write code here
   //you are here
}*/